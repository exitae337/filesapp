<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FilesApp" parent="Theme.Material3.DayNight.NoActionBar">
        // Light theme
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/main</item>

        <item name="colorAccent">@color/mainAccent</item>
    </style>

    <style name="ScreenBackground">
        <item name="android:background">@color/white</item>
    </style>

    <style name="CardBackGround">
        <item name="android:background">@color/mainAccent</item>
    </style>

    <style name="TextColor">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ImageBackground">
        <item name="android:tint">@color/white</item>
    </style>

    <style name="MainText">
        <item name="android:textColor">
            @color/black
        </item>
    </style>

    <style name="PlanetColor">
        <item name="android:startColor">@color/subTitle</item>
    </style>

    <style name="BackgroundTint">
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="Theme.FilesApp" parent="Base.Theme.FilesApp" />
</resources>